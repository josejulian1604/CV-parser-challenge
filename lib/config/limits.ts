// Resolved decision (PLAN.md): page cap at 3, confirmed as sound cost
// control. Char-count limiting isn't included here — no threshold value has
// been decided yet; adding one would be a guess, not a documented decision.
export const MAX_PAGES = 3;
